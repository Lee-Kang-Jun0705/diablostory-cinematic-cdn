# DiabloStory Cinematic CDN

Static cinematic files for DiabloStory runtime CDN builds.

The directory layout intentionally preserves `/assets/cinematic...` paths so the app can use this repository as `VITE_CINEMATIC_BASE_URL`.
